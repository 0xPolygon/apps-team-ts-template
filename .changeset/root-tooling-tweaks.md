---
---

Update root `.prettierignore` and `CLAUDE.md` for the hey-api codegen migration: ignore the generated client output and the auto-generated OpenAPI spec; document the codegen workflow and the `example-client` package role change.
