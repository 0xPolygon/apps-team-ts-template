---
---

Update template `.github/CODEOWNERS` to point at `@0xPolygon/product-applications`, the canonical apps-team handle for new repos. Repos cloned or assessed against this template will now match the handle that downstream tooling (e.g. the workspace `/assess-repo` skill) expects.
