# Migration Guide

This package is a reference implementation — it does not have a public API that consumers
need to migrate. Future entries here would cover changes to exported types or behaviour
that affect repos generated from this template.
